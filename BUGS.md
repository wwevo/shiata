* Pie chart becomes hard to read with many small-scale sections. The amounts should be outside the chart, possibly next to the legend on the right hand side.
* export page needs a "select all" checkbox per category
* recipe instances need to be set to "static" when components are altered **and** saved. 
* Changing a product correctly propagates down to change existing products. This doesn't work when a product is inside an instantiated recipe. 
